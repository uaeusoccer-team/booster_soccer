<root BTCPP_format="4">
  <BehaviorTree ID="MainTree">
    <ReactiveSequence name="head_track_no_search">
      <!-- No CamFindBall. Only track if vision already sees/knows the ball. -->
      <CamTrackBall theta="{tracking_theta}" />

      <!-- Keep body fully stationary. -->
      <SetVelocity />
    </ReactiveSequence>
  </BehaviorTree>
</root>
